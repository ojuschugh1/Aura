// Package proxy implements an MCP proxy that sits between AI tools and their
// MCP servers. Every tool call flows through Aura, giving full observability,
// real-time policy enforcement, automatic cost tracking, and live verification.
//
// This is the "Wireshark for AI agents" — see everything your agent does,
// verify it's true, and stop it before it breaks things.
package proxy
